# Handover — 2026-02-12 Session

## What Was Done

### 1. P1 Stability Fixes (commit `dc1a58f`)
- **SQLite WAL mode**: Added `PRAGMA journal_mode=WAL` to 6 files (7 connection points) for concurrent read support
- **Error boundaries**: Created `error.tsx` for 7 routes (global, jobs, translate, write, write-v2, dashboard, settings)
- **Loading states**: Created `loading.tsx` for 7 routes with skeleton UI
- **Silent exception fixes**: Fixed 3 bare `except:` and 2 silent `except Exception` patterns with logging

### 2. Screenplay Studio Sidebar Navigation (commit `ae92aeb`)
- Added "Screenplay Studio" to sidebar nav in `app-shell.tsx` (positioned below Write Pro)
- Added EN ("Screenplay Studio") and VI ("Kich ban") translations
- Imported `Clapperboard` icon from lucide-react
- Fixed `api.ts` to use `NEXT_PUBLIC_API_URL` env var instead of relative `/api/screenplay` path

### 3. Port Configuration
- Backend runs on port **4001** (ports 3000 and 4000 were occupied by other apps)
- Frontend runs on port **3001**
- `.env.local` updated: `NEXT_PUBLIC_API_URL=http://localhost:4001`

## Current State

### Git
- Branch: `main`
- Latest commit: `ae92aeb` — sidebar nav + API client fix
- Tag: `v3.3.0` on commit `8160305`
- 3 commits ahead of origin (not pushed)

### Running Services
- Backend: `uvicorn api.main:app --host 0.0.0.0 --port 4001`
- Frontend: `npx next dev -p 3001` (from `frontend/` directory)

### Known Issues
- **CORS**: Frontend on port 3001 calls backend on port 4001 cross-origin. CORS is configured in `config/settings.py` with dev defaults including both ports.
- **Next.js port conflict**: `npx next dev` from Claude Code sometimes picks up wrong project (OTBDAFC). Must run from correct `frontend/` directory with explicit `cd`.
- **Backend stability**: uvicorn occasionally hangs when receiving WebSocket HMR requests from Next.js. Restart if API calls timeout.

## Files Changed This Session

| File | Change |
|------|--------|
| `frontend/src/components/layout/app-shell.tsx` | Added Clapperboard icon + screenplay nav entry |
| `frontend/src/lib/i18n/translations.ts` | Added screenplay key (EN + VI) |
| `frontend/src/lib/screenplay/api.ts` | API_BASE uses NEXT_PUBLIC_API_URL env var |
| `frontend/.env.local` | NEXT_PUBLIC_API_URL=http://localhost:4001 |

## Quick Start
```bash
# Terminal 1 — Backend
cd /Users/mac/ai-publisher-pro-public
uvicorn api.main:app --host 0.0.0.0 --port 4001

# Terminal 2 — Frontend
cd /Users/mac/ai-publisher-pro-public/frontend
npm run dev -- -p 3001
```

Open: http://localhost:3001/screenplay
