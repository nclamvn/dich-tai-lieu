# Handover Document — 2026-02-11

## Session Summary
Book Writer v2 — frontend completion, pipeline bug fixes, parallel acceleration, and AI adapter fix.

## Commits This Session
```
475eb94 fix: AI adapter now extracts .content from Response object
5db3687 feat: Book Writer v2 — draft upload, parallel pipeline, UI fixes
```

## What Was Done

### 1. Frontend — "Continue from Draft" Feature (Completed)
The third agent from the previous session (frontend types, client, hooks) had 0 tool uses — it never executed. Completed all missing frontend work:

- **types.ts**: Added `continue_draft` to `InputMode`, `continue_from_draft`/`draft_file_id` to `BookV2CreateRequest`, new types (`DraftChapterInfo`, `DraftAnalysisResponse`, `DraftUploadResponse`)
- **client.ts**: Added `uploadDraft()` and `analyzeDraft()` methods to `bookWriterV2` client
- **hooks.ts**: Added `useUploadDraftV2()` and `useAnalyzeDraftV2()` mutation hooks
- **book-create-form.tsx**: Added mode toggle (From Scratch / Continue from Draft), draft upload UI, draft analysis display
- **translations.ts**: Added 11 EN + 11 VI i18n keys for draft feature

### 2. Bug Fixes
| Bug | Root Cause | Fix |
|-----|-----------|-----|
| TypeScript error after adding `continue_draft` | `Record<InputMode, ...>` required all keys | Changed to `Partial<Record<...>>` + optional chaining |
| Duplicate React key warnings | `part.number`/`ch.number` can collide across parts | Changed to `part.id`/`ch.id`; draft list uses array index |
| Empty detail page for "created" status | No UI rendered for `created` status | Added "Created — Waiting for pipeline..." card with pulsing indicator |
| Pipeline stuck at "created" | Service and pipeline created separate `BookProject` objects with different IDs | Made `pipeline.create_book()` accept optional `project` param; service passes its project |
| Bulk delete button invisible | Was above scroll viewport | Made bulk action bar sticky (`sticky top-0 z-10`) + red `variant="secondary"` |
| Content shows "Response object at 0x..." | `AIClientAdapter` did `str(response)` on `Response` object | Added `hasattr(response, 'content')` check to extract `.content` attribute |

### 3. Pipeline Parallelization (5x Speed Boost)
All 6 content-processing agents parallelized with `asyncio.Semaphore(5)` + `asyncio.gather()`:

| Agent | Before | After |
|-------|--------|-------|
| Outliner | Sequential loop | Parallel (5x) |
| Writer | Sequential loop | Parallel (5x) |
| Expander | Sequential loop | Parallel (5x) |
| Enricher | Sequential chapter loop | Parallel (5x) |
| Editor | Sequential section + chapter loops | Parallel (5x) |
| Publisher | Sequential format loop | All formats parallel |

Agents NOT parallelized (single AI call each): Analyst, Architect, QualityGate.

## Key Files Modified
```
# Backend
core/book_writer_v2/ai_adapter.py          — Response object fix
core/book_writer_v2/pipeline.py             — Accept existing project
core/book_writer_v2/agents/outliner.py      — Parallelized
core/book_writer_v2/agents/writer.py        — Parallelized
core/book_writer_v2/agents/expander.py      — Parallelized
core/book_writer_v2/agents/enricher.py      — Parallelized
core/book_writer_v2/agents/editor.py        — Parallelized
core/book_writer_v2/agents/publisher.py     — Parallelized
api/services/book_writer_v2_service.py      — Pass project to pipeline

# Frontend
frontend/src/lib/api/types.ts               — Draft types
frontend/src/lib/api/client.ts              — Draft API methods
frontend/src/lib/api/hooks.ts               — Draft hooks
frontend/src/components/book-writer-v2/book-create-form.tsx — Draft UI
frontend/src/lib/i18n/translations.ts       — Draft i18n
frontend/src/app/write/page.tsx             — Partial<Record> fix
frontend/src/app/write-v2/[id]/page.tsx     — Created status + key fix
frontend/src/app/jobs/page.tsx              — Sticky bulk actions
```

## Known Issues / Next Steps
1. **Test new book creation** — Previous books have corrupted content ("Response object at 0x..."). Create a new book to verify the adapter fix works end-to-end.
2. **Draft upload backend** — The `/api/v2/books-v2/upload-draft` and `/api/v2/books-v2/analyze-draft` endpoints need to be verified/implemented in the backend routes.
3. **PDF generation** — Currently falls back to Markdown. Could integrate a proper PDF library.
4. **Progress WebSocket** — Verify real-time progress updates work with the parallelized agents.

## Architecture Notes
- **Pipeline flow**: Analyst → Architect → Outliner → Writer → Expander → Enricher → Editor → QualityGate → Publisher
- **AI Adapter pattern**: `AIClientAdapter` wraps `UnifiedLLMClient` — must handle `Response` object with `.content` attribute (not just `dict`)
- **Concurrency**: All parallel agents use `CONCURRENCY = 5` constant with `asyncio.Semaphore`
- **Project identity**: Service creates `BookProject`, passes it to pipeline — single source of truth for project ID

## Servers
- Backend: `uvicorn api.main:app --port 8000 --reload`
- Frontend: `cd frontend && npm run dev` (port 3001, since 3000 was occupied)
